import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Link } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { 
  Plus, 
  Search, 
  BookOpen, 
  CloudSun 
} from "lucide-react";

export default function QuickActions() {
  const actions = [
    {
      title: "List Equipment",
      description: "Share your farming tools",
      icon: Plus,
      color: "bg-green-500 hover:bg-green-600",
      link: createPageUrl("Equipment")
    },
    {
      title: "Find Equipment",
      description: "Rent nearby tools",
      icon: Search,
      color: "bg-blue-500 hover:bg-blue-600",
      link: createPageUrl("Equipment")
    },
    {
      title: "Crop Guide",
      description: "Get planting advice",
      icon: BookOpen,
      color: "bg-purple-500 hover:bg-purple-600",
      link: createPageUrl("CropGuide")
    },
    {
      title: "Weather Details",
      description: "Extended forecast",
      icon: CloudSun,
      color: "bg-amber-500 hover:bg-amber-600",
      link: createPageUrl("Weather")
    }
  ];

  return (
    <Card className="bg-white/80 backdrop-blur-sm border-0 shadow-lg">
      <CardHeader>
        <CardTitle>Quick Actions</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="grid grid-cols-2 gap-4">
          {actions.map((action) => (
            <Link key={action.title} to={action.link}>
              <Button 
                variant="ghost" 
                className={`w-full h-auto p-4 flex flex-col items-center gap-3 ${action.color} text-white hover:text-white transition-all duration-200 hover:scale-105`}
              >
                <action.icon className="w-6 h-6" />
                <div className="text-center">
                  <div className="font-semibold">{action.title}</div>
                  <div className="text-xs opacity-90">{action.description}</div>
                </div>
              </Button>
            </Link>
          ))}
        </div>
      </CardContent>
    </Card>
  );
}
